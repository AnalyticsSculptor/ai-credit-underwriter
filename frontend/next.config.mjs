/** @type {import('next').NextConfig} */
const nextConfig = {
  serverExternalPackages: [], // Keep whatever else might be here
  allowedDevOrigins: ['172.19.16.1', 'localhost'],
};

export default nextConfig;